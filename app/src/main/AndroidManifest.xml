<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="uap.geocolportaje.geocoportaje">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ActivitiesPrincipales.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ActivitiesPrincipales.ventasActivity" />
        <activity android:name=".ActivitiesPrincipales.libroActivity" />
        <activity android:name=".FormulariosCreacion.nuevolibroActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".ActivitiesPrincipales.MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".FormulariosCreacion.nuevaubicacionActivity" />
        <activity android:name=".ActivitiesPrincipales.usuarioActivity" />
        <activity android:name=".FormulariosCreacion.nuevaplanillaActivity" />
        <activity android:name=".ActivitiesPrincipales.planillaActivity" />
        <activity android:name=".ListaSeleccion.listalibrosActivity" />
        <activity android:name=".ListaSeleccion.listaplanillasActivity" />
        <activity android:name=".Informes.informeplanillaActivity" />
        <activity android:name=".ActivitiesPrincipales.clienteActivity" />
        <activity android:name=".FormulariosCreacion.nuevoclienteActivity" />
        <activity android:name=".ListaSeleccion.listaclientesActivity" />
        <activity
            android:name=".FormulariosCreacion.nuevaventaActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".ListaSeleccion.listaventasActivity" />
        <activity android:name=".Informes.informeventaActivity" />
        <activity android:name=".Informes.informeclienteActivity" />
        <activity android:name=".ListaSeleccion.listapuntoActivity" />
        <activity android:name=".Informes.informepuntosActivity"></activity>
    </application>

</manifest>